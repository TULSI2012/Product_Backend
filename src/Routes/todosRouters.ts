import express from "express";

// import { getAllTodos, addTodo } from "../Controllers/todosControlls";

// const router = express.Router();

// // localhost:8000/todos/ GET, POST
// router.route("/").get(getAllTodos).post(addTodo).delete(deleteTodos);

// // localhost:8000/todos/:id
// // router.route("/:id").get(getAllTodos);

// export default router;